var G_VAR = require("G_VAR")
cc.Class({
    extends: cc.Component,

    properties: {
        ftype: 0; //类型
        fspeed : 0; //速度
        fdir: G_VAR.DIR_LEFT; //方向
    },

    // LIFE-CYCLE CALLBACKS:

    // onLoad () {},

    start () {

    },

    // update (dt) {},
});
